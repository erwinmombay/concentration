app.controller 'CardsCtrl', ($scope, LoginService) ->

  pair = []

  @onCardClick = (card) ->

  #$scope.gameCtrl.generate().then ((profiles) ->
    #@cards[0..] = profiles
  #).bind @

  return this
